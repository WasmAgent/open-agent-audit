{
  "name": "@openagentaudit/worker",
  "version": "0.1.0",
  "description": "OpenAgentAudit Cloudflare Worker reference deployment",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "files": ["src"],
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests yet' && exit 0",
    "clean": "rm -rf dist .turbo .wrangler"
  },
  "wasmagent": { "stability": "alpha" },
  "dependencies": {
    "@openagentaudit/core": "0.1.0",
    "@openagentaudit/adapters": "0.1.0",
    "@openagentaudit/schema": "0.1.0"
  }
}
