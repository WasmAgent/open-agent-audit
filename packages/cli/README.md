# @openagentaudit/cli

Local CLI for OpenAgentAudit, Bun-first.

**Status:** alpha skeleton. Production uses `@openagentaudit/worker`.

```bash
bunx openagentaudit --help
```

Implementation is blocked on the Phase 2 freeze gate; see
[`docs/schema-versioning.md`](../../docs/schema-versioning.md).
